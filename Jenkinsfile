import com.example.jenkins.*

// Go in via the pipelines class and invoke the desired pipeline. This 
// Keeps our pipeline code neat and tidy but also prevents repetition of logic.
Pipelines pipelines = new Pipelines()

pipelines.javaPipeline()
